{
  "name": "mineshare",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && pnpm run post-build",
    "build:watch": "vite build --watch",
    "post-build": "node build-extension.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mysten/sui": "^1.44.0",
    "@mysten/walrus": "^0.8.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.0"
  }
}
